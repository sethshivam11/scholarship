{
  "name": "scholarship",
  "version": "1.0.0",
  "description": "This is a scholarship to study abroad",
  "main": "index.js",
  "scripts": {
    "start": "node backend/index.js",
    "dev": "nodemon backend/index.js",
    "build": "npm install && npm run client-install && npm run client-build",
    "client-install": "cd frontend && npm install",
    "client-build": "cd frontend && npm run build"
  },
  "author": "shivam",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "nodemon": "^3.0.1"
  }
}
